{
  "name": "auth-passport-session-jwt",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=14.*"
  },
  "scripts": {
    "dev:web": "cd apps/web && yarn start",
    "dev:api": "cd apps/api && yarn start:dev",
    "prepare": "yarn prepare:web && yarn prepare:api",
    "prepare:web": "cd apps/web && yarn install",
    "prepare:api": "cd apps/api && yarn install"
  }
}
