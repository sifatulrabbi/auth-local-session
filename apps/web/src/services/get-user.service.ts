import React from 'react';
import { IUserPreview } from '../@types';
import axios from 'axios';

// export function useGetUser(user: IUserPreview | null): { user: IUserPreview } {
//   const user: IUserPreview | null = axios.get('http://localhost:5000/users/')
// }
