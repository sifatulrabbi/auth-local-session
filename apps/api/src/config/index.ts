import * as dotenv from 'dotenv';

dotenv.config();

export const config = {
  port: 3000,
  mongoDbUri: 'hello',
  sessionSecret: 'hello',
  emailAcc: 'hello',
  emailAccPass: 'hello',
  errors: 'hello',
};
